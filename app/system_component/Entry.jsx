import React, {Component} from 'react';
import {Table, Divider, Modal, List, Input, Button} from 'antd';

class Entry extends Component {

    render(){
        return (
            <div>
               设备录入
            </div>
        );
    }
}

export default Entry;